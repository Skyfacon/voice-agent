# ASR Streaming Eval Fixtures

This directory documents fixture policy only. Do not commit audio recordings
here.

The default harness path uses synthetic metadata fixtures. If future audio
generation is approved, generated files must stay local-only under
`/private/tmp` and should be removed after metadata extraction.
