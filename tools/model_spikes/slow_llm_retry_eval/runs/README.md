# Slow LLM Retry Eval Runs

Local run outputs belong outside the repository, normally under:

```text
/private/tmp/voice-agent-slow-llm-retry-eval/
```

Do not commit JSONL runs unless they are explicitly reviewed as
metadata-only, synthetic, and replay-safe.
