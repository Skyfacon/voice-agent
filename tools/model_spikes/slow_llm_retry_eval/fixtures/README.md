# Slow LLM Retry Eval Fixtures

This directory documents fixture policy only. Do not commit provider request
or response bodies here.

The default harness path uses synthetic metadata fixtures. Shareable output
must stay synthetic, redacted, and minimal.
